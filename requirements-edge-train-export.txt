# Reproducible edge train/export profile: preprocessing + training + export.
# Install with:
#   python -m pip install -r requirements-edge-train-export.txt
#   python -m pip install -e . --no-deps
-r requirements-edge-ops.txt
scipy>=1.11.0
pandas>=2.0.0
tensorflow>=2.14.0,<2.17.0
tf2onnx>=1.16.0
onnxruntime>=1.17.0
