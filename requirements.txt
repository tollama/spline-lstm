# Core
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# ML
# TensorFlow runtime guidance:
# - Python 3.10~3.11 is recommended for stable wheel/runtime compatibility.
# - Keep TF below 2.17 in this repo until runtime matrix is re-validated.
tensorflow>=2.14.0,<2.17.0
torch>=2.0.0

# Runtime compatibility
# - For Python builds linked against LibreSSL (common on older macOS system Python),
#   urllib3 v2 may emit NotOpenSSLWarning.
# - Use urllib3<2 in that environment, or prefer a Python build linked to OpenSSL>=1.1.1.
urllib3<2; python_version < "3.10"

# Data
paho-mqtt>=1.6.0

# Utils
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
pyyaml>=6.0

# Backend API (GUI contract skeleton)
fastapi>=0.110.0
uvicorn>=0.29.0
httpx>=0.27.0

# Dev
jupyter>=1.0.0
pytest>=7.4.0